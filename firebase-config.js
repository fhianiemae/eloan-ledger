// Paste your Firebase project's web config here (Firebase console → Project settings →
// General → Your apps → SDK setup and configuration → "Config"). This value is not a
// secret: access to your data is controlled by Firebase Authentication + firestore.rules,
// not by hiding these keys. See README.md for the full setup walkthrough.
export const firebaseConfig = {
  apiKey: "PASTE_YOUR_API_KEY",
  authDomain: "PASTE_YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "PASTE_YOUR_PROJECT_ID",
  storageBucket: "PASTE_YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "PASTE_YOUR_SENDER_ID",
  appId: "PASTE_YOUR_APP_ID"
};
